
# ── Soroban / WASM build artifacts ─────────────────────────────────────────
*.wasm
*.d

# ── Test snapshots & fuzz artifacts ─────────────────────────────────────────
**/__snapshots__/
*.snap
*.snap.new
*.insta.yaml

# Build artifacts
*.wasm
*.d
*.optimized.wasm

# Soroban / Stellar CLI generated files
.soroban/
.stellar/
contract-ids.json

# Fuzz corpus and artifacts generated during fuzzing runs
contracts/ttl_vault/fuzz/corpus/
contracts/ttl_vault/fuzz/artifacts/
contracts/ttl_vault/fuzz/target/

# Environment files
.env
.env.local
.env.*.local
.env.mainnet
.env.testnet

# ── Editor / IDE ─────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# ── Node (frontend / mobile tooling) ─────────────────────────────────────────
node_modules/
dist/
build/
.cache/
.parcel-cache/
*.tsbuildinfo

# ── Android build artifacts ───────────────────────────────────────────────────
mobile/android/.gradle/
mobile/android/app/build/
mobile/android/local.properties
mobile/android/**/*.apk
mobile/android/**/*.aab
mobile/android/**/*.keystore

# ── iOS / Xcode build artifacts ───────────────────────────────────────────────
mobile/ios/build/
mobile/ios/**/*.xcworkspace/xcuserdata/
mobile/ios/**/*.xcodeproj/xcuserdata/
mobile/ios/DerivedData/
mobile/ios/**/*.ipa
mobile/ios/Pods/

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log

# Coverage reports
lcov.info
tarpaulin-report.html
coverage/
*.profraw
*.profdata

# Temporary / misc
*.tmp
*.bak
target/
